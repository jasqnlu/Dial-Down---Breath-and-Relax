# Xcode
*.xcuserstate
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build
build/
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods
Pods/
Podfile.lock

# Swift Package Manager
.build/
.swiftpm/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Graphify knowledge graph (regenerate with `graphify update .`)
graphify-out/

# Python bytecode (Blender pipeline tooling)
__pycache__/
*.pyc

# Git worktrees (subagent-driven-development isolation)
.worktrees/

# Blender pipeline staging output — regenerated on every render, not
# deterministic byte-for-byte, and not what ships in the app (the app bundles
# finished clips from Resources/Animations/ instead; see
# Tools/blender/exercises/ANIMATION_HANDOFF.md).
Tools/blender/generated/

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Claude Code local settings (personal permission allowlist, machine-specific)
.claude/settings.local.json
